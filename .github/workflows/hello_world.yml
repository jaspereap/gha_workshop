# name
name: hello_world

# trigger
on:
- push

# one or more jobs
jobs:
    greetings:
        runs-on: ubuntu-latest
        steps:
        - id: s00
          run: echo "current time in Github $(date)"
        - name: Cowsays
          uses: mscoutermarsh/cowsays-action@v1
          with:
            text: 'ship ship ship!'
            color: 'blue'
        #   - id: s01
        #     run: ls -la